apiVersion: v1
kind: Service
metadata:
  name: {{ include "hubcast.fullname" . }}
  labels:
    {{- include "hubcast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "hubcast.selectorLabels" . | nindent 4 }}
